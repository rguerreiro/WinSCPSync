<UserControl x:Class="WinSCPSyncGui.Views.ServiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <TextBlock Text="STATUS:" Foreground="White" Margin="10 10 10 10" FontSize="20" />
            <TextBlock x:Name="Status" Foreground="White" Margin="10 10 10 10" FontSize="20"/>

            <Button x:Name="Start" Content="START" Margin="5 5 5 5" />
            <Button x:Name="Stop" Content="STOP" Margin="5 5 5 5" />
        </StackPanel>
    </Grid>    
</UserControl>